# Obsidian local workspace state — keep local only
.obsidian/workspace.json
.obsidian/workspace-mobile.json
.obsidian/cache
.obsidian/graph.json

# OS / editor cruft
.DS_Store
Thumbs.db
*.swp
.vscode/

# Autosync (headless) — Discord payload buffer, overwritten every run
.autosync-payload.tmp.json

# Autosync (headless) — upstream CHANGELOG scratch copy, overwritten every run
.cc-changelog.tmp.md
