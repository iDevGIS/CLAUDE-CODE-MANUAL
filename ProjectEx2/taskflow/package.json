{
  "name": "taskflow",
  "version": "0.1.0",
  "description": "Task management CLI + HTTP API — example project for advanced Claude Code features",
  "private": true,
  "type": "commonjs",
  "bin": {
    "taskflow": "./src/cli/index.js"
  },
  "scripts": {
    "start": "node src/cli/index.js",
    "serve": "node src/server/index.js",
    "test": "node --test 'tests/**/*.test.js'",
    "test:watch": "node --test --watch 'tests/**/*.test.js'",
    "lint": "node scripts/lint.js"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
